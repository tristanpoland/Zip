[package]
name = "simple-browser"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.24.1"
egui = "0.24.1"
webview = { version = "0.1.1"}

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3.9", features = ["winuser"] }